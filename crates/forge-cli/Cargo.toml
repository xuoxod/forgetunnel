[package]
name = "forge-cli"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Universal operator CLI for Forgetunnel: Sovereign Local-AI & Webhook Bridge"

[[bin]]
name = "forgetunnel"
path = "src/main.rs"

[dependencies]
forge-core = { path = "../forge-core" }
forge-telemetry = { path = "../forge-telemetry" }
forge-ledger = { path = "../forge-ledger" }
forge-report = { path = "../forge-report" }
forge-ai = { path = "../forge-ai" }
forge-webhook = { path = "../forge-webhook" }
forge-bridge = { path = "../forge-bridge" }
tokio = { workspace = true }
clap = { workspace = true }
colored = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
